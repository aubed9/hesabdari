version: '3.8'

services:
  arayeshi-erp:
    build: .
    container_name: arayeshi_erp
    restart: always
    ports:
      - "4200:4200"
    environment:
      - PORT=4200
      - NODE_ENV=production
    volumes:
      - ./db:/app/db
      - ./backups:/app/backups
